// global object is "similar" to window object of browser. The global object does not
// contain the document object inside the window and other such

// console.log(global);

// global.setTimeout is = setTimeout. We do not need to explicitly add global before
// everything.

console.log({__dirname});
console.log({__filename});